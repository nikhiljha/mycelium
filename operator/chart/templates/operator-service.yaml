apiVersion: v1
kind: Service
metadata:
  name: mycelium-operator
  namespace: mycelium
  labels:
    app: mycelium-operator
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: api
  selector:
    app: mycelium-operator